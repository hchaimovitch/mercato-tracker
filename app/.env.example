# Copie ce fichier en .env si tu veux forcer l'URL du backend (ex: test web,
# backend distant). Sans ce fichier, l'app détecte automatiquement l'adresse
# de ton ordinateur sur le réseau local via le serveur de développement Expo.
EXPO_PUBLIC_API_URL=http://localhost:4000
